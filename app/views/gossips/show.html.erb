<h1><%= @gossip.title %></h1>

<p><%= @gossip.content %></p>

<p><strong>Auteur :</strong> 
  <% if @gossip.user.present? %>
    <%= link_to "#{@gossip.user.first_name} #{@gossip.user.last_name}", user_path(@gossip.user) %>
  <% else %>
    Auteur inconnu
  <% end %>
</p>

<p><em>Publié le <%= @gossip.created_at.strftime("%d %b %Y à %H:%M") %></em></p>

<hr>

<%= link_to "Retour aux potins", gossips_path %> |
<%= link_to " Modifier", edit_gossip_path(@gossip) %> |
<%= link_to " Supprimer", gossip_path(@gossip), data: { turbo_method: :delete, turbo_confirm: "Es-tu sûr de vouloir supprimer ce potin ?" } %>

